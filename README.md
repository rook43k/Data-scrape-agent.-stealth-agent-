# Data-scrape-agent.-stealth-agent-
This agent uses public and custom APIs to scrape data from various financial institutions. The agents uses attributes like selenium and playwright to emulate human character when interacting with the sites to avoid being detected and sending requests at human rate.
